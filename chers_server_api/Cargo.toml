[package]
name = "chers_server_api"
version = "0.1.0"
edition = "2021"

[dependencies]
chers = { path = "../chers" }
serde = { version = "1", features = ["derive"] }
ts-rs = { version = "10", features = ["serde-compat"] }

[lib]
crate-type = ["cdylib", "rlib"]
