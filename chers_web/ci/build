#!/usr/bin/env bash
set -e

# Use Vercel's cached Rust dependencies for faster builds
export CARGO_HOME=/vercel/.cargo

# Vercel runs from chers_web/, need to go up to project root for workspace
cd ..

mise run web-release
