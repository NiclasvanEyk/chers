#!/usr/bin/env bash
set -e


# Vercel runs from chers_web/, need to go up to project root for workspace
cd ..

curl https://mise.run | sh
mise install

# Install wasm target for Rust (not managed by mise)
rustup target add wasm32-unknown-unknown
